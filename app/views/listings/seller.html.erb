
    
      <table class="table table-striped table-bordered">
    <thead>
          <tr>
            <th class="center success">Image</th>
            <th class="center success">Name</th>
            <th class="center success">Description</th>
            <th class="center success">Price</th>
               <th class="center success">Seller</th>
              <th class="center success">Manage</th>
          </tr>
        </thead>

          <tbody>
                <% @listings.each do |listing| %>
          <tr>
            <td><%= image_tag listing.image, :width => 100, :height => 100  %></td>
              <td><%= listing.name %></td>
            <td><%= listing.description %></td>
            <td><%= number_to_currency listing.price, unit:'£' %></td>
            <td><%= listing.user.name %></td>
              <td>
            <div class="btn-group">
  <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    Action <span class="caret"></span>
    </button>
  
  <ul class="dropdown-menu">
    <li><%= link_to 'Edit', edit_listing_path(listing), class: "btn btn-link" %></li>
      <li role="separator" class="divider"></li>
    <li> <%= link_to 'Destroy', listing, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link" %></li>
      <li role="separator" class="divider"></li>
    <li><%= link_to 'Show', listing, class: "btn btn-link" %></li>
      </ul>
    </div>
    </td>
    </tr>
    <% end %>
    </tbody>
    </table>
        
    