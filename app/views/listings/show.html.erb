
<div class="row">

<div class="col-md-6">
	<div class="thumbnail">
	<%= image_tag @listing.image %>
</div>
</div>

<div class="col-md-6">
    <h3> <%= @listing.name %></h3>
<p> <%= number_to_currency @listing.price, unit: '£' %></p>
<p> <%=  @listing.description %></p>
    
    <div class="center">
        <%= link_to 'Buy Now', new_listing_order_path(@listing), class: "btn btn-success" %>
</div>
</div>
</div>

<% if current_user == @listing.user %>


<%= link_to 'Edit', edit_listing_path(@listing), class: "btn btn-link" %> 
 

<% end %>
<%= link_to 'Back', listings_path, class: "btn btn-link" %>
 <p><%= image_tag "credit-card.png", :width => 400, :height => 100 %></p>