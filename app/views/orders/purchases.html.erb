<div class="center">
<h2>Your Purchase History</h2>
</div>
    
      <table class="table table-striped table-bordered">
    <thead>
          <tr class="center success">
            <th>Image</th>
            <th>Item</th>
            <th>Price</th>
            <th>Seller</th>
               <th>Date Sold</th>
              
          </tr>
        </thead>

          <tbody>
              <% @orders.each do |order| %>
          <tr>
              <td class="center"><%= image_tag order.listing.image, :width => 100, :height => 100  %></td>
              <td class="center"><%= order.listing.name %></td>
            
              <td class="center"><%= number_to_currency(order.listing.price, unit:'£') %></td>
              <td class="center"><%= order.seller.name %></td>
              <td class="center"><%= order.created_at.strftime("%B %-d, %Y") %></td>
              </tr>
              <% end %>
    </tbody>
    </table>
        
    