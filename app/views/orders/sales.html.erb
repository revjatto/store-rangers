<div class="center">
<h2>Your Sales History</h2>
</div>
    
      <table class="table table-striped table-bordered">
    <thead>
          <tr class="center success">
            <th>Image</th>
            <th>Item</th>
            <th>Price</th>
            <th>Customer</th>
               <th>Date Sold</th>
              
          </tr>
        </thead>

          <tbody>
              <% @orders.each do |order| %>
          <tr>
              <td><%= image_tag order.listing.image, :width => 100, :height => 100  %></td>
              <td><%= order.listing.name %></td>
            
              <td><%= number_to_currency(order.listing.price, unit:'£') %></td>
              <td><%= order.buyer.name %></td>
              <td><%= order.created_at.strftime("%B %-d, %Y") %></td>
              </tr>
              <% end %>
    </tbody>
    </table>
        
    